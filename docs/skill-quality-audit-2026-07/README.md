# Skill quality audit — 2026-07-01 (working document)

Maintainer-facing work package. Not part of the plugin's runtime — none of the skills reference this folder. Delete the folder when the work is done.

## What this is

A full-plugin quality assessment of all 22 skills plus the repo's own authoring conventions, measured against:

- Anthropic, "Skill authoring best practices" — https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
- Anthropic, "The Complete Guide to Building Skills for Claude" (PDF) — https://resources.anthropic.com/hubfs/The-Complete-Guide-to-Building-Skill-for-Claude.pdf

**Method:** 52 agents — one assessor per skill + 4 convention-level auditors, each followed by an adversarial verifier that (a) re-checked every quote against the files, (b) screened every redundancy claim against `scripts/validate.sh` and `test/expected-outputs.yaml` enforcement, (c) refuted taste-only findings, and (d) hunted for missed high-severity issues. 4 findings were refuted (listed per skill in [findings.md](findings.md) so they don't get re-raised). Snapshot: branch `chore/skills-quality-assessment` at `8cef3e4` (clean tree), 2026-07-01.

**Headline results:** no skill needs a rebuild — every diagnosis supports targeted refinement. The single highest-leverage fix is convention-level (see [C1](#c1-the-description-convention-optimizes-a-mechanism-the-plugin-disables) below). The collection's structure, progressive disclosure, and feedback-loop discipline are genuinely strong; the surviving problems concentrate in **edge-of-flow correctness** (failing-test/rollback paths in harness mode, safety promises without verification gates, destructive paths without precondition checks) and a handful of **cross-file contradictions**.

## How to work on these improvements (read this first)

1. **Read the prerequisites** before editing anything: `.claude/CLAUDE.md`, `.claude/docs/skill-writing-guidelines.md` (mandatory before skill/agent/reference edits), `CONTRIBUTING.md`. For changes to `scripts/` use `.claude/docs/coding-guidelines.md` instead.
2. **Pick a work item** from the ranked list below, then read only that item's section in [findings.md](findings.md) — it is large (~250k chars); never read it wholesale. Each section is self-contained: quotes, locations, problem statements, concrete fixes, and best-practice citations (BP ids resolve in the [reference section](#best-practice-reference) below).
3. **Re-locate every quote before editing.** Line numbers in findings were captured at `8cef3e4` and will drift; the verbatim quote is the anchor — grep for it. If a quote no longer exists, the finding may already be fixed or the file restructured: re-verify the problem before acting.
4. **Check token pinning before touching any quoted line.** Many contract strings are pinned by `scripts/validate.sh` or `test/expected-outputs.yaml` (e.g. tdd/workflow→pr summary headings, brainstorm↔tdd scenario tokens, how-to-run's "guided in-chat walkthrough", handoff's `### Inlined (not yet on remote)`). Grep both files for any phrase you're about to change; several fixes explicitly name tokens to preserve. Findings that *looked* like removable duplication but are test-enforced were already refuted — trust the surviving list, but re-check enforcement whenever your edit deviates from the proposed fix.
5. **Verifier verdict semantics:** `CONFIRMED` — implement the proposed fix. `ADJUSTED` — the verifier's revised severity/fix **supersedes** the assessor's; implement the revised version. `REFUTED` — do not implement (kept in findings.md for the record). `Found by verifier` items are as real as confirmed ones (they passed the same evidence bar).
6. **Diagnose before trusting, even here.** These findings were adversarially verified, but you are a third reader: if the code/markdown contradicts a finding, believe the file and note the discrepancy in this README's changelog rather than forcing the fix.
7. **Repo mechanics (non-negotiable):**
   - Run `bash scripts/validate.sh && bash scripts/test-hooks.sh && python -m pytest test/harness-common/` after every batch of edits.
   - Bump the version in `.claude-plugin/plugin.json` and the README badge together, once per meaningful published change.
   - Never leave a `ref` field in `marketplace.json` on master.
   - After any skill change, verify the skill's `README.md` and the root `README.md` row still match behavior (validate.sh checks listing, not wording — wording drift is on you, and several findings below are exactly that).
   - If you touch `skills/permissions/templates/hooks/restrict-paths.sh`, re-copy it into this repo's own `.claude/hooks/` — the live copy has drifted 3× before.
8. **Style of remedy:** diagnose honestly, remedy conservatively. Prefer the leanest fix that resolves the finding — when a finding offers two remedies, the maintainer's standing preference is the lean / high-model-latitude option. Don't restructure adjacent content while passing through (the guidelines' own "drive-by improvements" anti-pattern).
9. **Suggested batching:** one skill (or one convention) per commit/PR. Conventions first — C1 especially, since every future description edit should be written against the corrected standard. Within skills, follow the ranking. Cross-skill findings (C6) touch multiple files — keep each as its own coherent commit.
10. **Track progress in this file:** add a `## Changelog` entry per completed item (date, item id, commit) so a later session knows what's done.

## Ranking — improvement need, highest first

Scores are the adversarially verified 0–10 improvement-need scores (0–2 well-tuned, 3–4 minor polish, 5–6 moderate, 7–8 structural, 9–10 rebuild). No skill scored above 6; no rebuilds.

| # | Skill | Verified score | Dominant problem class |
|---|-------|:---:|------------------------|
| 1 | unit-test | 6 | missing guidance on failing-test paths (breaks harness contract) |
| 2 | permissions | 5 | unverified security-critical install; silent overwrite of user hook edits |
| 3 | handoff | 5 | redaction promise not actually enforced end-to-end |
| 4 | code-review | 5 | agents never given the diff; PR mode can review wrong local content |
| 5 | jira | 5 | factually wrong MCP tool tables disable features |
| 6 | init | 5 | monorepo testing.md placement contradiction |
| 7 | tdd | 5 | several advertised behaviors don't fire as written |
| 8 | how-to-run | 5 | over-specification; worst-in-repo description (1021/1024 chars) |
| 9 | workflow | 4 | destructive Discard advice without clean-tree precondition |
| 10 | reset | 4 | cleanup can disable hooks the user chose to keep |
| 11 | unit-test-deep | 4 | red-baseline silent rollback loop |
| 12 | refactor-deep | 4 | --resume bypasses the green-baseline gate |
| 13 | pr | 4 | post-rebase push has no failure-mode guidance |
| 14 | prompt | 4 | contradictory CoT guidance across references |
| 15 | spec-init | 4 | multi-repo branch silently breaks the core handoff promise |
| 16 | refactor | 4 | its guideline agent skips the skill-authoring lens |
| 17 | commit | 4 | secrets-rule conflict; message generated before untracked decision |
| 18 | worktree | 4 | shared guard embeds contradicting policy; failing baseline unhandled |
| 19 | brainstorm | 3.5 | well-tuned; prose-deliverable routing quirk; spec overwrite check |
| 20 | code-review-deep | 3 | well-tuned; `--force` self-contradiction |
| 21 | branch | 3 | well-tuned; next-step advice contradicts own README |
| 22 | commit-message | 3 | well-tuned; near-exemplary minimal skill |

Convention topics (full detail in findings.md under "Convention-level audits"): descriptions-layer 4, authoring-conventions 5, shared-architecture 5, cross-skill-redundancy 5.

## Convention-level findings (higher leverage than any single skill)

These repeat across every skill, so fix them first. Full verified findings for each live in findings.md; this is the synthesis.

### C1. The description convention optimizes a mechanism the plugin disables

`.claude/docs/skill-writing-guidelines.md` "Description Quality" says *"Claude uses descriptions to select the right skill from potentially many available skills"* / *"they are injected into the system prompt"*, and Progressive Disclosure says *"Load only metadata (description) at startup."* With `disable-model-invocation: true` on all 22 skills, none of that applies: descriptions' real audience is a **human scanning the truncating `/` menu**. The convention drives keyword-stuffed near-cap descriptions (how-to-run **1021/1024** chars — and validate.sh checks presence only, so the next added capability breaks the platform cap silently; jira 710, workflow 707, refactor 704).

**Fix:** rewrite the Description Quality section for the real audience — lead with the differentiating verb phrase, target ~250–450 chars, keep what+when accuracy against actual behavior — and correct the "Load only metadata at startup" bullet to the plugin's actual loading model (description surfaces in the `/` menu and listings; SKILL.md loads at explicit invocation).

**Scope caution:** this fix targets the repo's OWN `.claude/docs/skill-writing-guidelines.md`. The *template* at `skills/init/templates/docs/skill-writing-guidelines.md` ships to user projects where model invocation may be enabled — its upstream-style advice is correct there. At most add a conditional note to the template ("if your skills set disable-model-invocation, write descriptions for the human menu"); do not port the rewrite wholesale.

Then re-tune the worst descriptions against the corrected standard (each has a finding in its skill's section): how-to-run (cut to ~350–450 chars, keep the validate.sh-pinned "guided in-chat walkthrough" phrase), refactor (three restatements of the two-goal framing), tdd (undeclared git side effects: branch/auto-commit/push; no /optimus:workflow pointer), unit-test-deep (missing "Requires a test command in .claude/CLAUDE.md" its two siblings carry), refactor-deep + code-review-deep (fresh-subagent fact stated twice each), spec-init (only "Use when…"-first description in the set — flip to verb-first), brainstorm + jira (name `docs/specs/` / `docs/jira/` save locations, which sibling skills treat as public API).

### C2. Guideline text contradicts the collection it governs

(a) *"Prefer gerund form for skill names: `processing-pdfs`, `analyzing-code`"* — all 22 actual names are short verb/noun slash-commands. Codify the real convention (names surface as `/optimus:<name>`). (b) The guidelines' Next Step section (*"Every skill must end with a recommendation… increases plugin adoption"*) contradicts `references/skill-handoff.md`, which allows *"'none' if the chain ends here"*. Align on the handoff doc; replace the adoption rationale with the user-value rationale. (c) Three SKILL.md-content rules (emoji ban, no [Step N/M] progress lines, imperative fan-out counts) live only in CONTRIBUTING.md — move or cross-reference them into skill-writing-guidelines.md, which is what the doc-loading path actually routes to.

### C3. Closing-tip wording exists in two mechanisms with no guard

~12 skills use the pointer form ("use Variant A per skill-handoff.md") while **18 verbatim inline copies** sit across 8 skills, unpinned by validate.sh — the exact drift pattern skill-handoff.md exists to prevent. Fix: converge on the pointer form, or add a validate.sh assertion that every `**Tip:**` line in `skills/*/SKILL.md` matches a canonical variant. (All 18 copies currently match — the risk is future drift, so the check is the lean fix.)

### C4. The two-tier agent extension pattern has an unresolved semantics gap

Plugin-level `agents/code-simplifier.md` says *"Direct simplifications — apply automatically"*; `agents/test-guardian.md` says run the test suite. Every skill-level extender that does `Read $CLAUDE_PLUGIN_ROOT/agents/<name>.md for your approach` is read-only; only code-review's code-simplifier wrapper carves the conflict out. Fix: add a "when read as an extension base, the dispatching prompt's constraints override the operational sections here" note to both plugin agents (and drop the one-off carve-outs, or keep them as reinforcement). Relatedly: no interactive SKILL.md states the prompt-assembly rule for fan-out agents (substitute the resolved plugin root; inline or absolutize bare `shared-constraints.md` references) even though the deep-mode dispatch templates state subagents don't inherit `$CLAUDE_PLUGIN_ROOT`. State it once at the pattern's home (`references/agent-architecture.md`) and point the launch steps at it.

### C5. references/harness-mode.md contradicts itself — under all three *-deep skills

Section 7's *"Skip any verification or validation step the base skill's normal (interactive) flow would perform"* countermands its own step 4 (*"Apply the same validation protocol as the skill's normal validation step"*) — and finding-validation is the only false-positive gate before deep mode auto-applies fixes. Fix: rescope to execution-based checks only, mirroring coverage-harness-mode.md's phrasing ("test-running/build/lint verification is skipped; finding validation still applies"). Also: its skill-step guidance is written in code-review vocabulary (branch-diff path, PR-description injection) that maps to nothing in refactor's steps, while the CLI populates `pr_description` for refactor-deep runs and refactor's context-blocks.md says the block "does not apply" — split the guidance into per-skill bullets or scope the pr_description paragraph to skills that define a PR/MR block.

### C6. Same procedure, different statements across skills

- **Branch-name collision:** three policies among five `branch-naming.md` consumers (branch/worktree auto-append `-2` via verbatim-duplicated blocks; commit asks the user; tdd/workflow silent). Add a Collision handling section to `skills/commit/references/branch-naming.md` as the single default and convert inline copies to pointers. Also fix its stale consumer list (omits `workflow`) and commit/README's "(shared with TDD)".
- **tdd multi-repo chimera:** *"process each repo independently: run Steps 1–9 inside the repo the user is targeting"* fuses unit-test's all-repos policy with brainstorm's single-repo policy — self-contradictory in one clause. Adopt brainstorm's wording.
- **Deep-skill CLI init/resume docs diverge:** code-review-deep tells users to re-invoke *the skill* with `--force` (a flag its own Step 1 doesn't parse, contradicting its line 112); siblings say re-invoke `init` with `--force`. Hoist shared cli.py init/resume semantics into a shared reference parameterized by progress-file path; keep per-skill deltas inline.
- **prerequisite-check divergence:** the shared reference prescribes warn-and-continue fallbacks; the three -deep skills hard-stop; unit-test/tdd/brainstorm/workflow inline their own stops. Document the strictness split centrally in prerequisite-check.md rather than unifying behavior.
- **CONTRIBUTING.md drift:** its project tree lists 20/22 skills (missing `spec-init/`, `handoff/`) and omits `sdd-mapping.md`. Optionally extend validate.sh's stale-README check to cover it.

## Already well-tuned — do not churn

- **commit-message** — near-exemplary minimal skill; the model for lightweight skills.
- **branch, code-review-deep, brainstorm** — small targeted fixes only.
- **The descriptions as a set** — uniform verb-first voice, consistent side-effect declarations, base→deep cross-pointers on all three pairs. Fix the named outliers; don't restyle the rest.
- **Deliberate redundancy confirmed deliberate** (verified against validate.sh/expected-outputs): orchestrator-loop single/paired overlap (paired defers to single as canonical, deltas carry inline rationale), handoff/tdd token-pinned templates, most of init's point-of-action overwrite-rule restatements. Leave these alone.
- Widely-praised patterns worth preserving as-is: tdd's Iron Law + horizontal-slicing diagram, code-review's Step 3 auto-route table and Step 6 validation loop, jira's MCP-safety architecture, how-to-run's anti-hallucination audit pairing, reset's fingerprint tables, the -deep skills' CLI-contract fidelity (verified against cli.py).

## Best-practice reference

Findings cite these ids. Distilled from the two Anthropic sources; treat as the shared vocabulary, not new rules to enforce wholesale.

- **BP1 Conciseness** — context is a public good; only add what Claude can't infer; every paragraph must justify its token cost.
- **BP2 Degrees of freedom** — match specificity to fragility: high freedom (text guidance) when many approaches are valid; low freedom (exact commands) only where fragile/sequence-critical. Default high.
- **BP3 Cross-model robustness** — instructions must work for weaker models (enough guidance) and stronger ones (no over-explaining).
- **BP4 Description field** — WHAT + WHEN, third person, ≤1024 chars, specific key terms; negative triggers where confusable with a sibling. (In this repo, tuned for the human `/` menu — see C1.)
- **BP5 Naming** — specific verb/noun names, consistent across the collection.
- **BP6 Body size** — SKILL.md under 500 lines; split when approaching.
- **BP7 Progressive disclosure** — SKILL.md as overview/pointers; reference content organized by consumer so irrelevant context isn't loaded.
- **BP8 Reference depth** — official: one level from SKILL.md (partial-read risk); this repo deliberately allows two (validate.sh-checked). Flag >2 or demonstrated partial-read risk.
- **BP9 ToC for long references** — files >100 lines need a table of contents.
- **BP10 Workflows** — clear sequential steps; copyable checklist for complex flows.
- **BP11 Feedback loops** — validator → fix → repeat; validation as a gate.
- **BP12 No time-sensitive info** — no date-conditional logic; "old patterns" sections for deprecated behavior.
- **BP13 Consistent terminology** — one term per concept; no contradictory statements of the same rule.
- **BP14 Template pattern** — strict templates only where consistency is critical; flexible otherwise.
- **BP15 Examples pattern** — input/output pairs where style matters.
- **BP16 Conditional workflows** — explicit decision points; exhaustive branches; large branches in separate files.
- **BP17 One default, not many options** — a default plus an escape hatch.
- **BP18 Forward slashes** — no Windows-style paths.
- **BP19 Scripts** — solve don't punt; no unexplained constants; execute-vs-read intent explicit.
- **BP20 Plan-validate-execute** — verifiable intermediate plan before batch/destructive/high-stakes operations.
- **BP21 Specific and actionable** — exact command + expected output + failure fixes; no vague verbs.
- **BP22 Instruction placement** — critical rules near the top / at point of action; buried rules get missed.
- **BP23 Error handling** — common failure modes and recovery steps for operations that fail in known ways.
- **BP24 Evaluation-driven** — iterate from observed behavior, not assumptions.
- **BP25 Composability** — a skill works alongside other skills; handoffs must match the receiving skill's actual behavior.

**Finding categories:** over-specification, under-specification, ambiguity, redundancy, missing-guidance, weak-description, structure, doc-drift, convention-conflict, other. **D9** = README↔SKILL.md drift.

## Changelog

*(add entries here as items are completed: date — item — commit)*

### Session 1 status (2026-07-01)

- **Done:** C1 (`315a2a7`) → #1 unit-test (`a5b832a`) → #2 permissions (`3bad8fa`) → #3 handoff (`4150cea`) → wrap-up/version bump to 2.0.20 (`34004d8`), in that order, all on `chore/skills-quality-assessment` (local only, **not pushed**). All three suites green at `34004d8` (validate.sh 17 passed — jq checks SKIP on this machine because jq isn't installed, that's environmental, not a regression; test-hooks 45 passed; pytest 410 passed).
- **Remaining:** conventions C2–C6, then skills #4 code-review through #22 commit-message per the ranking. No file contradicted a finding this session; the only deliberate deferrals are listed in the C1 entry below.
- **Version bumping for later sessions:** 2.0.20 covers this batch only. Bump again (once) for the next published batch.

### Session 2 status (2026-07-01 → 2026-07-02) — audit complete

- **Done (in order):** C2 (`8178dde`) → C3 (`3d29c58`) inline; then C4 (`b70b5f2`), C5 (`90e64b8`), SA-F6 (`e1cafd7`), C6 (`7dca0f6`, `ae87222`, `2c05fc5`, `2dcb349`, `e0d18ce`, `ec482a9`) via a sequential 7-agent workflow; auth-F5 (`db6e740`) + auth-F7 (`c3ff5d9`) inline; then skills #4–#22 via a sequential 19-agent workflow (one commit per skill, `15abb5d` … `8488b72`); version bump to 2.0.21 (`31f14dc`). All on `chore/skills-quality-assessment`, local only, **not pushed**.
- **Every ranked item and convention finding is now closed** — implemented, superseded by an earlier commit (recorded as a skip in its per-item entry), or REFUTED. No open findings remain.
- All three suites green at `31f14dc`: validate.sh 18 passed (two new checks added this session — closing-tip variant pinning and CONTRIBUTING structure-tree coverage; jq checks SKIP because jq isn't installed — environmental), test-hooks 45 passed, pytest 410 passed.
- **Notable file-vs-finding discrepancies (all recorded in per-item entries):** SA-F6's "annotate agent-architecture.md as non-runtime" half was invalidated mid-session by C4 making it runtime-consumed; #11's F1 revised fix was adapted to session 1's `blocked`-gate machinery; #12/#20 quotes had moved into the new `references/harness-init-resume.md` (C6-cli hoist); #16's F3 was already resolved by C5's pr_description scoping.
- This work package is complete. Per the header note, the `docs/skill-quality-audit-2026-07/` folder can be deleted once the maintainer no longer needs the record.

### Cross-item: findings already completed by C1 — do not redo

C1's description re-tunes satisfy the matching **weak-description findings inside other skills' findings.md sections**. When you work these items, skip their description finding (grep the section for "weak-description" / "description"; the quotes below no longer exist):

- **#5 jira** — description now names `docs/jira/` ("structured task saved to docs/jira/ for downstream skills to auto-detect"); trailing "or to refresh existing context after JIRA edits" clause dropped to keep length ~flat (712 chars).
- **#7 tdd** — description now declares spec auto-detection (docs/specs/ / docs/jira/), branch creation, per-behavior commits, push, and the /optimus:workflow fan-out-peer pointer.
- **#8 how-to-run** — its F3 (description 1021 chars) is done: cut to 474, "guided in-chat walkthrough" kept; the 1825-char root-README row is also already trimmed (desc-audit F2). Its F1/F2/F4–F8 remain open.
- **#11 unit-test-deep** — description now carries "Requires a test command in .claude/CLAUDE.md." Note also that #1's fixes added a `blocked` JSON field + orchestrator termination rule (coverage-harness-mode.md §2/§5, orchestrator-loop-paired.md step 3) — re-verify #11's red-baseline finding against those changed files before implementing.
- **#12 refactor-deep** — its F3 (duplicate fresh-subagent sentence) is done: replaced with the auto-apply differentiator + /optimus:refactor back-pointer. Its F1 (incl. the description's "project-wide"), F2, F4–F7 remain open.
- **#15 spec-init** — description flipped to verb-first (WHAT before "Use when…").
- **#16 refactor** — its F5 (704-char description) is done: cut to 426, single two-goal statement. F1–F4, F6, F7 remain open.
- **#19 brainstorm** — description now names `docs/specs/` + tdd/workflow auto-detect; root README brainstorm row aligned to `docs/specs/`.
- **#20 code-review-deep** — its F4 (duplicate fresh-subagent sentence) is done: sentence deleted. F1–F3, F5–F8 remain open.
- **Convention audit "The 22 frontmatter descriptions as a set"** — all seven findings (F1–F7) are done via C1.
- **Convention audit "Authoring conventions vs upstream best practices"** — F1 and F8 are done via C1; **F2–F7 are open** (they are C2, C3, C5-adjacent, and CONTRIBUTING-drift items).

Also done in #1 unit-test but living in shared files other items consume: `references/coverage-harness-mode.md` (cycle-context spec moved in, stop-gate rule + `blocked` schema field added, measurement-run clarification) and `references/orchestrator-loop-paired.md` (blocked-gate termination rule). Re-locate quotes in those files before editing them for C5 or the deep-skill items.

### Per-item entries

- 2026-07-01 — **C1** (description convention + worst-description re-tunes) — commit `315a2a7`. Rewrote the guidelines' Description Quality section for the human `/`-menu audience and corrected the "Load only metadata at startup" bullet (auth F1+F8); added the conditional-note-only change to the init template per the scope caution. Re-tuned descriptions: how-to-run (1021→474 chars, kept validate.sh-pinned "guided in-chat walkthrough"), refactor (704→426), tdd (+side effects/spec detection/workflow pointer), unit-test-deep (+test-command prerequisite), refactor-deep (dup fresh-subagent sentence → auto-apply differentiator + /optimus:refactor pointer), code-review-deep (dup sentence deleted), spec-init (verb-first), brainstorm + jira (+docs/specs/ / docs/jira/ save locations, jira kept ~flat at 712). Added a description-length (≤1024) check to validate.sh's frontmatter loop (desc-audit F1 ADJUSTED — handles single-line and folded `>-` scalars). Trimmed the root README how-to-run row 1825→~540 chars (desc-audit F2) and aligned the brainstorm row's spec path. **Deliberately deferred:** gerund-naming bullet left in place in the rewritten section (that's C2a); refactor-deep's "project-wide" lead-in left for skill item #12 (tied to its Step 1/README scope-model fix F1).
- 2026-07-01 — **#1 unit-test** (all 9 findings; F2/F3 in ADJUSTED form) — commit `a5b832a`. F1+F8: abandon and bug branches of the per-test workflow now revert the failing test and record `fail-abandoned` (+`failure_reason` naming the bug); F2: Final verification marked normal-mode-only, harness recording moved into the per-test branches; F3: stop gates under harness mode now emit the Step 6 JSON with a new `blocked` schema field (coverage-harness-mode.md) and orchestrator-loop-paired.md step 3 got a blocked-gate termination rule (orchestrator-instruction variant chosen over a cli.py change — leaner, no code path touched); F9: one post-write coverage-instrumented run explicitly permitted as measurement for `coverage.after`, verification-gating still forbidden; F4: "only add new test files" → "only add new tests (new files or appended cases)" in constraint/intro/description/README; F5: cycle-context block spec moved to coverage-harness-mode.md §2, pointer left in SKILL.md; F6: duplicate scope sentence folded; F7: README "5-step"→"6-step" + full shared-references list.
- 2026-07-01 — **#2 permissions** (all 9 findings; F7 in ADJUSTED form) — commit `3bad8fa`. F8: hook verify is now a concrete gate (diff against template + `bash -n`, re-copy on mismatch; re-applied Step 3 customizations are the only allowed delta); F9: Step 3 diffs an existing hook before overwriting and offers re-apply/discard via AskUserQuestion; the report bullet and skill README "Extending with custom patterns" note updated to match; F1: precious-file scan moved before the report so items 1–4 are one contiguous pre-report checklist; F3: the 29-pattern find command replaced with derive-from-`is_precious()` instruction (hook is single source of truth); F5: literal 13/30 counts → "every entry from the template", branch bullet reads `PROTECTED_BRANCHES` from the installed hook; F2: six paragraph report bullets compressed to one-liners + README section pointers (all topics kept); F4: description now names git branch protection and precious-file safeguards; F6: "see step 2 above" → "see the permissions.deny merge rule above"; F7: malformed-settings.json rule added to Merge principles. Hook template untouched — no `.claude/hooks/` re-copy needed.
- 2026-07-01 — **#3 handoff** (F1–F7; refuted /optimus:commit-parenthetical finding left alone) — commit `4150cea`. F6: redaction widened from inlined-only to the entire document body (Step 5 retitled "Redact the drafted document"; table intro now "everything written into the document", reference lines exempt); F1: Step 6 re-scans the written file post-write and reports "redaction scan re-ran clean" instead of the unconditional safety claim; F7: Continue-one now adopts the chosen file's slug for the rest of the run; F2: template annotations converged on bracket style ("— omit if none" moved off the pinned heading to a bracketed line, author-facing anchor blockquote bracketed, "(omit if none)" labels bracketed, template intro states brackets are never emitted); F3: omission-policy restatements dropped from Step 4 and the template intro (kept in Step 1 + placeholders); F4: root policy now resolves the single-child-repo and no-structure detection outcomes; F5: README "document is biased toward it" claim trimmed to match actual behavior. All validate.sh handoff tokens preserved.
- 2026-07-01 — **wrap-up** — commit `34004d8`. Version 2.0.20 in `.claude-plugin/plugin.json` + root README badge; changelog commit hashes recorded.
- 2026-07-01 — **C2** (guidelines vs the collection they govern; auth F2+F4+F6) — commit `8178dde`. F2: gerund-naming bullet dropped from Description Quality; real convention (short verb/noun names, surface as `/optimus:<name>`) codified in Foundation's Intention-Revealing Names bullet with a pointer left in Description Quality. F4: Next Step mandate rewritten to require skill-handoff.md's three-part closing (conversation/mode/next skill) and explicitly allow "none" for terminal skills; "increases plugin adoption" replaced with the user-value rationale. F6: emoji ban, [Step N/M] ban, and imperative fan-out rules moved verbatim into the guidelines' Writing Style section (the "move" option — single source, no duplication); CONTRIBUTING.md's "Output tone and formatting" section now points there. Template guidelines untouched per the C1 scope caution (its gerund line is upstream-correct for user projects). Auth F3 (tip-wording dual source) deferred to C3; auth F5 (depth-check blind spots) and F7 (CONTRIBUTING tree drift) deferred to their own items.
- 2026-07-01 — **C3** (closing-tip drift guard; auth F3 + cross-skill F5 ADJUSTED) — commit `3d29c58`. Chose the lean check option over converging 18 inline sites: validate.sh now asserts every conversation-hygiene `**Tip:**` line in `skills/*/SKILL.md` matches Variant C verbatim or the Variant A/B invariant text with wildcarded placeholder slots (per the verifier's revised fix); non-closing tips (the two deep-mode "Single-pass…" pointers) are excluded by the "contains 'conversation'" classifier. Negative-tested (injected one-word drift → FAIL, reverted). Guidelines' Next Step bullet no longer restates the tip wording — it now says emit the applicable skill-handoff.md variant verbatim. All 18 inline copies left in place (all currently match; now pinned).
- 2026-07-01 — **C4** (two-tier agent extension semantics; shared-arch F1+F2+F4) — commit `b70b5f2`. F1: both plugin agents (agents/code-simplifier.md, agents/test-guardian.md) got a "When read as an extension base" note — the dispatching prompt's constraints (read-only, scope, test execution, output format) override the operational sections, only quality criteria/focus areas carry over; code-review's one-off carve-out kept as reinforcement per the lean choice. F2: prompt-assembly rule stated once at the pattern's home ("Prompt assembly at dispatch time" in references/agent-architecture.md — substitute the resolved absolute plugin root into every agent prompt, inline-or-absolutize bare shared-constraints.md); code-review Step 5, refactor Step 4, and tdd's quality-gate.md launch step each got a one-sentence pointer+gist; orchestrator-loop-single.md's dispatch clause now reads "base SKILL.md, harness-mode.md, or the agent prompt files they load" and the line-56 commentary points at the new rule (paired-loop's identical clause left for its consumers — refactor SKILL.md's own launch step now carries the instruction). F4: chose the finding's primary (leaner) fix over the verifier-endorsed skill-level addendum — one carve-out sentence in references/shared-agent-constraints.md permitting the project's existing test/coverage commands only "when your agent prompt's role explicitly requires it", so read-only agents stay strict. F3/F5/F7/F8 untouched (item C5); F6 untouched (own item).
- 2026-07-01 — **C5** (harness-mode.md self-contradiction + fork + ToC + pr_description scoping; shared-arch F3+F5+F7+F8) — commit `90e64b8`. F7: step 7's blanket "Skip any verification or validation step" rescoped to test-running/build/lint checks only with "finding validation (step 4) still applies", mirroring coverage-harness-mode.md's narrow intent; F3: the inline Iteration Context Block fork (which dropped the status legend, empty-field fallbacks, and the "Focus on NEW issues only / Do NOT re-flag" closer) replaced with a pointer to context-injection-blocks.md as the single source plus the two harness-specific deltas (no pre/post_edit_content in the block; Failed Fix Attempts sourced from accumulated-findings' fix_description/last_failure_hint); F5: "Skill-step execution under harness mode" added to both Contents lists (file-level entry 2 with the pr_description note; nested bullet under step 1 in the inner 1–9 list, matching its physical position); F8: pr_description paragraph scoped to skills that define a PR/MR context block, with the "refactor ignores config.pr_description — its agents/context-blocks.md states the block does not apply" clause (lean prose option; cli.py untouched). No quote drift found — session 1's coverage-harness-mode.md edits didn't disturb the F7 anchor. F1/F2/F4 were C4 (`b70b5f2`); F6 left for its own item.
- 2026-07-01 — **SA-F6** (validate.sh orphan-check vacuity for root references/ and agents/) — commit `e1cafd7`. Check 9 tightened: root references/ and agents/ files now require a full-path or basename match in skills/ — the parent-directory fallback (vacuous for those dirs, since "references/" and "agents/" match trivially somewhere in skills/) is kept for skill-level files only; negative-tested with a dummy root reference (FAIL, reverted), all 12 existing root files pass so no exemption list was needed. **Discrepancy:** the finding's second remedy (annotate references/agent-architecture.md as a contributor doc exempt from runtime consumption) no longer applies — since C4 (`b70b5f2`) code-review SKILL.md:177, refactor SKILL.md:125, and tdd quality-gate.md:15 load it at runtime via `$CLAUDE_PLUGIN_ROOT` as the home of the prompt-assembly rule, so the file is skill-consumed and passes the tightened check on its own; annotation skipped, no exemption added.
- 2026-07-01 — **C6-collision** (branch-name collision consolidation; cross-skill F3+F8) — commit `7dca0f6`. F3: Collision Handling section added to skills/commit/references/branch-naming.md as the single default policy (verbatim text of the former branch/worktree blocks: git show-ref, append `-2`…`-9`, stop); the two inline copies (branch SKILL.md Step 3, worktree SKILL.md Step 3) replaced with pointers to that section; commit SKILL.md step 6 keeps its report-and-ask behavior but now states it as a deliberate divergence from the reference's default (user pre-approved the exact name in step 5); tdd Step 3 / workflow Step 3 read-instructions extended with "and collision handling" so the previously-silent consumers inherit the default explicitly. F8: reference's consumer list now includes `workflow` (all five named); commit README's "(shared with TDD)" row corrected to "convention and collision handling (shared with the branch, tdd, workflow, and worktree skills)". No discrepancies — all quoted anchors matched the current files; no validate.sh/expected-outputs pins on any touched phrase; branch README's "appends a numeric suffix automatically" bullet still matches behavior (policy unchanged, only relocated).
- 2026-07-01 — **C6-small** (tdd multi-repo chimera + prerequisite-check strictness note; cross-skill F2 + F7 ADJUSTED) — commit `ae87222` + `2c05fc5`. F2: tdd SKILL.md Step 1's self-contradicting multi-repo clause ("process each repo independently: run Steps 1–9 inside the repo the user is targeting") replaced with brainstorm's exact current single-repo wording ("process within the repo the user is targeting. If ambiguous, ask which repo."); tdd README's "targets specific repos in multi-repo setups" bullet already matches, no README change needed. F7 (verifier's revised fix): one-line strictness note added to the end of prerequisite-check.md's Fallback Behavior section (analysis skills use the fallbacks; generative and deep skills deliberately hard-stop on missing CLAUDE.md, inline stops override) — all inline hard-stops left in place, no consumers rerouted. No discrepancies — both quotes verbatim in the current files; no validate.sh/expected-outputs pins on either phrase.
- 2026-07-01 — **C6-cli** (deep-skill CLI init/resume doc hoist + interrupt caveat; cross-skill F1 + F4) — commit `2dcb349` + `e0d18ce`. F1: shared cli.py init/resume semantics (resume invocation + cap raising, init error recovery, CLI-level `--force` discard, `--no-commit` persistence, `.done.json` archival) hoisted — verified against scripts/harness_common/cli.py, which also contributed a fourth likely init error ("Working tree has uncommitted changes") — into new `references/harness-init-resume.md` parameterized by `<progress-path>`/`<cap-flag>` per the orchestrator-loop-single.md pattern; the three -deep Step 4s now point there via `$CLAUDE_PLUGIN_ROOT` (check 8 tracks it, depth 1, 33 lines so no ToC) and keep only their per-skill init invocations and deltas (refactor-deep `--focus` rejection, unit-test-deep `--allow-red` baseline), fixing code-review-deep's re-invoke-"this skill"-with-`--force` self-contradiction (now: re-invoke the CLI `init` subcommand with `--force`); the new reference was added to the three skill READMEs' structure tables and the inventories in .claude/CLAUDE.md, .claude/docs/architecture.md, and CONTRIBUTING.md (only the new file added there — the pre-existing sdd-mapping/spec-init/handoff drift stays with its own item). F4: the interrupt caveat sentence added verbatim from code-review-deep:71 to refactor-deep's and unit-test-deep's Step 3 warning blockquotes (lean option; warning blocks otherwise untouched). No discrepancies — all quoted anchors verbatim in the current files; no validate.sh/expected-outputs pins on any touched phrase; code-review-deep's closing "## Tip" resume/archival guidance left in place (user-facing, consistent with the new reference).
- 2026-07-01 — **C6-planmode** (brainstorm/jira plan-mode carve-out hoist; cross-skill F6) — commit `ec482a9`. F6: the hand-duplicated plan-mode carve-out block (the "## How this conversation should run" prompt closer + the 3-step user-instruction blockquote) hoisted from brainstorm SKILL.md Step 7 and jira SKILL.md Step 6 into a new "Carve-out canonical blocks" section of references/skill-handoff.md, parameterized by `<doc-path>` only (heading placeholder dropped as the leaner option — both skills standardized on jira's exact `### Refined plan`, which jira-refresh.md:64 and jira-codebase-analysis.md:110 protect by literal string, resolving brainstorm's unspecified heading level); each SKILL.md keeps its prompt-body fields inline and replaces the two duplicated blocks with pointers (a bracketed in-block placeholder for the prompt closer, matching the template's existing bracket style, and a one-line "tell the user … verbatim" instruction for the 3 steps); skill-handoff.md's line-78 "their inline plan-mode prompts already encode review-only…" sentence updated to point at the canonical blocks. No discrepancies — both quoted anchors verbatim in the current files; no validate.sh/expected-outputs pins on any touched phrase (the tip-drift guard, "## Scenarios"/"### Scenario:", and handoff-skill tokens all untouched); skill-handoff.md lands at 99 lines, under the 100-line ToC threshold; brainstorm/jira READMEs describe the flow at a level unaffected by the relocation.
- 2026-07-01 — **auth-F5** (depth-check mitigation claim) — commit `db6e740`. Chose the doc-correction option over extending the depth check (the harness-mode→context-injection-blocks chain through top-level references/ is deliberate architecture — an extended check would flag it): the guidelines' Progressive Disclosure note now names both blind spots (prose references untracked; top-level references//agents/ treated as leaves) instead of overstating the validate.sh safety net.
- 2026-07-01 — **auth-F7** (CONTRIBUTING structure-tree drift) — commit `c3ff5d9`. Added spec-init/ and handoff/ to the skills tree and sdd-mapping to the references/ line (harness-init-resume was already added by C6-cli); took the optional hardening too since the drift has recurred: validate.sh's README-consistency check now also asserts every skills/<name>/ directory appears in CONTRIBUTING.md's project-structure block.
- 2026-07-01 — **#4 code-review** (F1, F2 residual, F4–F7; F3+F8 skipped as already done) — commit `15abb5d`. F1: Step 5 agent prompts now carry the diff hunks (or per-file changed line ranges for oversized diffs) after the file list — never file paths alone; F2: C4's prompt-assembly pointer (`b70b5f2`) already covered root-substitution and shared-constraints inlining, so only the residuals were added to Step 5 (strip prompt-file frontmatter, file-list+diff last so the context blocks' "before the file list line" anchor resolves, resolve guideline-reviewer's dispatcher-facing "Dynamic Prompt Construction" section at assembly time); F4: "captured in Step 1" → "captured during scope determination" and "[PR/MR title/body from Step 1]" → "[captured …]" in references/context-injection-blocks.md, and the skill-level agents/context-blocks.md usage notes (all three duplicated the shared reference) trimmed to a pointer plus the Step 3 mapping; F5: Step 3 local flow gained a base-detection-failure fallback (ask for a base ref and use it as `<ref>` in Branch/ref mode, or report nothing to review — never guess); F6 (verifier's revised fix): dropped the duplicate "Rename the `Code:`/`Fix:` fields…" sentence from bug-detector and security-reviewer PR/MR addenda, keeping the canonical shared-constraints.md rule and the per-field Output Format parentheticals; F7: explicit PR/MR mode now compares `headRefOid` (GitHub) / `sha` (GitLab) against `git rev-parse HEAD` and offers `gh pr checkout <N>` / `glab mr checkout <N>` on mismatch, with a decline-path warning that validation reads the local tree. **Skipped as already resolved:** F3 (test-guardian carve-out) — C4's extension-base note in agents/test-guardian.md already excludes the test-run/coverage steps from carrying over; F8 (harness-mode step 7 blanket skip) — C5 (`90e64b8`) already rescoped it with "finding validation (step 4) still applies". No discrepancies; no validate.sh/expected-outputs pins on any touched phrase; skill README and root README row already match the fixed behavior (F7 makes the README's teammate-PR use case hold as written).
- 2026-07-01 — **#5 jira** (all 8 findings: F1 ADJUSTED, F2–F7, F8 found-by-verifier; description finding already done via C1) — commit `4c0d2d5`. F1: sooperset column corrected in both the Tool Name Resolution and MCP Safety permitted-write tables (`jira_add_comment`, plus audited '—' cells `jira_get_all_projects`, `jira_get_transitions`, `jira_add_worklog`) and SKILL.md Step 5's gate reworded to check the permitted add-comment tool named in the MCP Safety table via ToolSearch (whitelist stays normative — "do not probe for any comment-like tool"); F8: `jira_create_issue_link` added to both tables, the false "sooperset does not expose link creation" parenthetical deleted, "(Rovo only)"/"(Rovo)" qualifiers dropped from jira-implementation-tickets.md Linking + skill README, and the Linking step generalized to the table's create-link tool with a get-link-types-if-available schema check (create-without-link fallback kept for servers genuinely lacking a link tool); F2: generic-server paragraph added to Tool Name Resolution — pattern-map reads via ToolSearch, writes fail closed unless a discovered tool unambiguously matches a permitted purpose; F3: the drifted inline template (missing `- Labels:`) deleted from SKILL.md Step 4, reference named as single source (lean delete option — Step 3 already loads it); F4: MCP Safety added to the reference's Contents list (items renumbered 1–7); F5: tech-debt route now names classification signals (issue type, `tech-debt`/`refactor` labels, behavior-preserving goal); F6: project-key regex anchored to `^[A-Z][A-Z0-9]{1,9}$` with matching "2-10" gloss; F7: "For non-Complex scope, proceed to Step 6." appended to the comment-report item. No discrepancies — every quote verbatim at current line positions (C6-planmode's hoist didn't touch the anchors; "### Refined plan" contract untouched); no validate.sh/expected-outputs pins on any touched phrase; root README jira row already matches (no Rovo-linking claim).
- 2026-07-01 — **#6 init** (all 10 findings: F1–F6, F9+F10 found-by-verifier, F7+F8 in ADJUSTED form) — commit `4304a0f`. F9: test-infra-provisioning.md's Testing documentation section now carries explicit monorepo placement (per-subproject `docs/testing.md` for subprojects with test infrastructure; root `.claude/docs/testing.md` only for root-as-project), the CLAUDE.md-references section no longer points a non-root-as-project monorepo root at an uncreated root testing.md, the Monorepo-subprojects section references files "(created above)", and SKILL.md Step 6's placement pointer states the same semantics; F1: Step 6 gained an "Update the Documentation section" instruction (add created styling.md/architecture.md to the scoping CLAUDE.md; testing.md handled by Step 5b), Step 7 gained the reverse created→listed cross-check, and Step 4's "actually created" wording became "actually exist" with a first-run-starts-empty note; F2: Step 7 conditional warning added — after scaffolding created a `<name>/` subdirectory, tell the user to start future sessions there or CLAUDE.md/hooks won't load; F10: Before You Start scope corrected to "(hooks and `coding-guidelines.md`)" plus a template-derived-docs-are-NOT-generated clause; F7 (revised fix): only the line-70 inventory parenthetical and line-121 Step 2 sentence condensed to Before You Start pointers — point-of-action statements at Steps 2 (exception), 5, and 6 kept; F8 (revised fix): Step 6 skill-writing-guidelines row now reads "Skill authoring detected in Step 1 (structural rule in project-analyzer.md task 9)" and the checkpoint's stale "(also applied in the Step 6 install table)" parenthetical was dropped; F6: precise test-infra criteria moved into project-analyzer.md task 8 (yes/no report), Step 5b and the checkpoint branch on the flag; F3: Step 1b auditor launch prepends shared-constraints.md, mirroring Step 1; F4: multi-repo local-only note reworded to inform-at-creation (Detection Summary is closed by Step 4); F5: format-dart.sh added to templates/settings.json (JSON validity checked manually — jq SKIP on this machine). No discrepancies — every quote verbatim at current positions; no validate.sh/expected-outputs pins on any touched phrase; repo's own .claude/ copies left decoupled per item guidance; skill README and root README rows already match (abstraction level unaffected).
- 2026-07-01 — **#7 tdd** (F1–F4, F6, F7, F9–F11; F5 + F8 skipped as already done) — commit `9c581fa`. F1: coverage-baseline detection and Before run moved to Step 1's Verify-test-infrastructure subsection, Step 9's template note now runs only the After measurement and computes the delta (`### Coverage` handoff token untouched); F2: Step 4 gained the symbol-doesn't-exist stub escape hatch (minimal stub so the run fails on the assertion; scaffolding, not an Iron Law violation); F3 (lean reword option): the push-failure line now says fix, push manually, then `/optimus:pr` — a new invocation starts fresh, no resume — and the unfinished-behaviors line suggests a follow-up run from the feature branch with the remaining behaviors as the task; F4: milestone-boundary prompt moved from Step 3 into Step 7 with explicit ask-exactly-one precedence over the Next-step question, both Stop-here routes now proceed to Step 8's quality gate, the backwards "confirmation above" pointer became "in Step 3", and the doubled `[N]` split into `[M]` (milestone) / `[N]` (behaviors); F6: regression-gate revert now `git stash push -u -- <implementation-files>` plus a git status/diff verify-the-revert beat, and the Step 5 circuit-breaker Skip option (verifier's same-blindspot note) tells Claude to delete newly created untracked implementation files; F7: line-162 reproduce-the-bug restatement deleted (strategies bullet + scenario-path check cover both paths; confirmed unpinned); F9: Commit-remaining-work runs the test suite first (skip per project convention mirroring the circuit breaker, or surface the failure) and the Stats "Tests passing" line reports the actual last-run result instead of hardcoded "all ✓"; F10 (lean option): tdd-worktree-orchestration.md's Cleanup parenthetical reworded to match SKILL.md Step 9's actual order (final subsection, after the `/optimus:pr` recommendation); F11 (lean option): README "Submodule exclusion" bullet deleted — no instruction surface exists. **Skipped:** F5 — already resolved by C4's extension-base note in agents/code-simplifier.md (`b70b5f2`), same precedent as #4's F3 skip; F8 — description re-tune done via C1 (`315a2a7`) per the cross-item list (note: C1's description declares the git side effects and the /optimus:workflow pointer but not F8's /optimus:unit-test negative trigger; skipped per the binding cross-item instruction). No discrepancies — every quote re-located verbatim in the current files (line drift only); no validate.sh/expected-outputs pins on any touched phrase; scenario tokens, TDD-summary headings, and the pointer-form closing tip all untouched; root README tdd row and skill README still match behavior (F11 was the only drift).
- 2026-07-01 — **#8 how-to-run** (F1, F2, F4, F6–F8; F5 in ADJUSTED form; F3 skipped as already done) — commit `ccc0684`. F1: step6-verification-audits.md's Build Debug+Release pair now accepts a `-DCMAKE_BUILD_TYPE=Debug`/`=Release` configure-step pair for single-config CMake generators (Ninja / Unix Makefiles — the Linux default, where `--build --config` is a no-op), and how-to-run-sections.md's Build default skeleton renders that pair for those generators (pinned "Build Debug+Release pair" / "Default skeleton — multi-configuration build systems" tokens untouched); F2: Step 6's `/optimus:commit` bullet is now guarded to single project / monorepo with an explicit multi-repo fall-through (the workspace-root file is not version-controlled per Step 5), so the closing tip lands on Variant C via the existing "Otherwise" rule; F4: the four detection tables (Signal → Section Mapping, Additional Detection Hints, Build System Detection, Source Dependencies Detection, ~90 lines) split out of the 771-line how-to-run-sections.md into new `references/detection-signals.md` (90 lines, under the ToC threshold) — Step 1 now passes only that file to the sonnet detector, Step 4 reads it for the mapping plus the slimmed sections file for templates, the detector agent's four pointers and the skill README (feature bullet + structure table, split into two rows) updated, and validate.sh's ADH/BSD heading checks, BSD table-body token loop, and the 'Components table (Task 5d)' / 'Runtime Ports table (Task 5c)' pins moved to the new file; F5 (verifier's revised fix): Step 1 Checkpoint inlines the evaluable trigger labels (`local-windows-auth` / `local-named-instance` / `local-socket`) and drops the §Pre-Conditions Block §Trigger cross-file pointer (full trigger still re-evaluated at Step 4); F6: fallback-procedure references renamed to "fallback step 2/3/4" to stop colliding with skill Step 4, and the LLM-knowledge paragraph now requires inferred commands to pass the same fallback step 3 validation; F7: item 10's restated `<name>`/`<password>`/`<user>` substitution mechanics and MongoDB row-selection rule replaced with the existing pointer to §Verify Commands (seeds) (pinned '**Verify bullets:**' and 'Stale-tag re-validation' kept; MONGO_INITDB_ROOT_USERNAME pin lives in the esd file only); F8: the placeholder-substitution/slug algorithm moved from Step 4 item 5 into external-services-docker.md §Snippet Templates as a "Placeholder substitution" rule next to the templates it fills, one-line pointer left in item 5. **Skipped:** F3 — description re-tune done via C1 (`315a2a7`, 1021→474 chars) per the cross-item list; the root-README row trim was also already done there. No discrepancies — every quote re-located verbatim (line drift only); cross-step identifier counts preserved (approved-unverifiable-items 3×, rendered_line 3× in SKILL.md; stale-info headings untouched); anti-hallucination audit pairing preserved. All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#9 workflow** (all 4 findings: F1, F3, F4; F2 in ADJUSTED form) — commit `089f601`. F1: Step 3 gained a clean-tree precondition before `git checkout -b` — if `git status --porcelain` reports anything, stop and have the user commit or stash first, with the rationale that everything uncommitted from that point is treated as workflow output (Step 7 stages it, Step 5's Discard path deletes it); the Discard bullet itself is untouched since the precondition makes its assumption true (the finding's primary remedy — the verifier called the warning-only alternative a weak mitigation); F2 (verifier's revised fix): Step 1 table now shows the actual install paths (`.claude/docs/coding-guidelines.md`, `.claude/docs/testing.md` — the existing Monorepo Scoping Rule pointer covers the per-subproject case), the brief's bare `testing.md`/`coding-guidelines.md` mentions became `<resolved … path from Step 1>` slots, Step 4's fill paragraph instructs substituting the resolved paths and dropping absent docs, and the Edit-mode block adds the third clause — the forbidden list is an edit boundary, not a read boundary, so agents may read the constraint docs inside `.claude/`; F3: description reworded to "Requires /optimus:init; takes a spec (auto-detects docs/specs/ or docs/jira/), a spec path, or an inline goal" (matches argument-hint, Step 2 inline fallback, README usage); F4: Step 2's restated Distillation mechanics (threshold/single-sentence/AskUserQuestion confirm and the source re-enumeration the reference already carries) trimmed to "apply the shared reference's Distillation step — the result becomes the brief's `Goal:` slot in Step 4". Skill README "How It Works" step 3 updated for the clean-tree requirement; refuted Skill-Structure-table finding left alone; root README workflow row needed no change (claims neither a spec requirement nor tree state). No discrepancies — every quote verbatim at current positions (C6-collision's "and collision handling" edit was already in Step 3 and untouched); the pinned Implementation-Summary headings and pointer-form closing tip untouched. All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#10 reset** (all 8 findings: F1, F2, F4, F6; F3 + F5 in ADJUSTED form; F7 + F8 found-by-verifier) — commit `3f1df10`. F7: Step 4 now removes a hook's settings.json entry only if the referenced hook file was deleted in Step 5 or does not exist, so 'Keep modified'/'Unmodified only' no longer silently unhook a kept hook (restrict-paths.sh guardrail included); the Step 3 plan's settings.json line notes kept hooks stay wired, the Step 7 report says so explicitly, and the README's PostToolUse/PreToolUse bullets state the conditional removal — the 'User-added hooks → preserved' promise now holds; F1: MODIFIED reworded to 'differs from current templates … user-modified, or installed by an older optimus version whose templates have since changed' in the classification summary, the Step 3 plan heading ('## Modified (user edits, or older optimus version)'), and the README table, plus a Step 3 instruction to show `.claude/.optimus-version` as version-skew evidence; F2: Step 1 hooks inventory changed from the closed 8-name list to a `.claude/hooks/format-*` pattern scan (explicitly including init's unsupported-stack `format-<language>.sh` fallbacks, matching Step 4's prefix cleanup), and Step 2 gained the no-template classification rule (LIKELY_GENERATED via the shell-hook pattern, else MODIFIED) with the README format-* row updated; F3 (verifier's revised fix): the AskUserQuestion options now mark exactly one '(Recommended)' conditionally — Remove all only when every MODIFIED file is git-tracked, otherwise Keep modified naming the untracked MODIFIED files — and the README confirmation bullets state the same condition; F8: skill-writing-guidelines.md moved from the heading heuristic to the near-exact strategy (line-2+ comparison against the init template; section retitled 'Near-exact templates (coding-guidelines.md, skill-writing-guidelines.md)'), heuristic-table row and prose doc list dropped, README row updated; F4: CLAUDE.md heuristic row's headings cell replaced with '(not used — comment-only check…)', matching the prose; F6: `mcp__*` removal narrowed to exact-form server-level `mcp__<server-name>` entries for servers declared in the relevant `.mcp.json` (tool-level entries and undeclared servers preserved), README bullet tightened to match; F5 (verifier's revised fix — annotation only): Step 4 marked as a procedure executed during Step 5 item 2 and skipped on Abort, with the 'Step 4' cross-references in Safety Rules and Step 2 left intact. Fingerprint tables otherwise untouched per the preserve note; restrict-paths.sh template untouched — no `.claude/hooks/` re-copy needed. No discrepancies — every quote verbatim at current positions; no validate.sh/expected-outputs pins on any touched phrase; closing tip untouched; root README reset rows already match (classification + always-asks claims unaffected). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#11 unit-test-deep** (F1 ADJUSTED-adapted, F3–F5, F7, F8 found-by-verifier, F6 residual; F2 skipped) — commit `e08ec6b`. F1: re-verified against the current coverage-harness-mode.md/orchestrator-loop-paired.md per the item guidance — audit #1's blocked machinery (`a5b832a`) already kills the burn-to-the-cap scenario for pre-existing failing tests (the phase's Step 2 stop gate emits `blocked` and the paired-loop step 3 gate terminates at cycle 1), so the revised fix's "silently rolled back every cycle" warning text would now be false for that branch; implemented an adapted two-sentence version at the baseline: on `baseline-red-allowed` with existing tests (Step 2 infra check flagged neither gap) warn that the blocked gate ends the run at cycle 1, and on a timed-out baseline — the branch the blocked machinery does NOT cover, since cmd_unit_test_step's timeout still red-rolls every cycle printing only `continue` — warn of the silent rollback loop; both confirm before entering the loop. F3: `parse-failure` added to Step 5's termination enum. F4: overview "up to two subagent dispatches ... when untestable items are pending" + description "and, when untestable code is flagged," (closing ## Tip left as a worst-case cost statement per the verifier note). F5 (doc-correction option): coverage-harness-mode.md line 112 now says the binding focus is CLI-pinned `config.focus` (prompt mentions it only in prose) and cli.py's `_make_coverage_progress` comment drops the nonexistent "Focus: testability" line reference (comment-only cli.py change, explicitly named by the finding). F6: prerequisite sentence was done via C1 as instructed, but the finding's second half (the "or the cycle cap" elision) was NOT — fixed it ("is reached") while editing the description for F4; noted as a C1 cross-item-list gap. F7: README convergence row + "or none actionable". F8 (lean orchestrator-instruction option, no CLI token change): step 4 table row notes `continue` also covers a red-suite rollback; step 10 marks unit-test counts subagent-reported/not-CLI-confirmed, with commit-mode `nothing-to-commit`-despite-reported-tests as the rollback tell (record zero and say so). **Skipped:** F2 — resolved by C6-small (`2c05fc5`): prerequisite-check.md's strictness note now states deep skills hard-stop on missing CLAUDE.md and inline stops override the fallback section, so the guess the finding diagnosed no longer exists and a per-skill restatement would be redundant. No validate.sh/expected-outputs pins on any touched phrase (tip-drift guard untouched); root README row's "alternates ... per cycle" left as-is (loop-design abstraction, not an unconditional-dispatch claim); skill README already stated the conditional refactor phase. All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#12 refactor-deep** (F1 ADJUSTED, F2, F4, F6, F5 ADJUSTED + F7 found-by-verifier in shared/mirrored files; F3 skipped) — commit `36ab6b6`. F7 (the dominant --resume baseline bypass): the "Skip on `--resume`" baseline rule in refactor-deep Step 4 is now conditional on the prior run having completed at least one iteration (checked via a targeted read of the single `iteration.completed` field, findings array kept out of context); on `iteration.completed == 0` — baseline-red stop or an interrupt at/before iteration 1 — the baseline re-runs after `resume` (green required, or `--allow-red` per the user) before the loop; mirrored verbatim in code-review-deep Step 4 and, since C6-cli hoisted the line-109 recovery guidance, as a one-sentence note on the shared harness-init-resume.md "progress file already exists" bullet (generic wording — "apply the skill's baseline step's `--resume` rule" — stays consistent with unit-test-deep's unconditional-allow-red skip); the "(fresh runs only)" heading qualifier dropped in both skills. F1: the real scope rule stated once in Step 1 rule 7 (no path scope → iteration 1 covers the feature-branch diff when one exists, else full project; scope widens per iteration only to active-finding + newly modified files, per cli.py:575-580 + findings.update_scope), the contradicting "full branch diff is still processed" clause and "full project" bare-invocation example aligned, the description's "project-wide" lead-in (deferred from C1) replaced with "of the feature-branch diff (full project when no diff exists)" (516→603 chars, under cap), and README lines 23/32 aligned. F2: baseline-red stop now tells the user to fix the tests or accept red via `--allow-red-baseline` and continue via `--resume` (which re-establishes the baseline per F7's rule), and disambiguates the CLI message's internal `--allow-red` flag; `--allow-red-baseline` added to the README usage list. F4: argument-hint gains `[--no-commit]` and `[--allow-red-baseline]` (quoted form preserved for validate.sh). F5 (revised fix): implemented in references/harness-init-resume.md — **discrepancy:** the quoted sentence no longer exists in either SKILL.md (C6-cli `2dcb349` hoisted it, reproducing the same "higher cap" ambiguity in the shared file), so the fix landed there once instead of mirrored twice: pass `<cap-flag> N` whenever supplied; the CLI clamps to the hard cap and refuses at-or-below-completed only after a cap-terminated run. F6: Step 6's archival-semantics sentence replaced with a pointer to orchestrator-loop-single.md "After the loop" (command kept; confirmed unpinned). **Skipped:** F3 — done via C1 (`315a2a7`) per the cross-item list. Root README refactor-deep row left as-is ("Iterative project refactor" is the skill title, not a scope claim; the row makes no branch/project-scope assertion). No validate.sh/expected-outputs pins on any touched phrase; closing tip untouched. All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#13 pr** (all 9 findings: F1–F4, F6, F5 + F7 in ADJUSTED form, F8 + F9 found-by-verifier) — commit `d9d795b`. F8 (the dominant post-rebase push hole): Step 4's already-on-remote push now runs divergence detection (`git rev-list --count HEAD..origin/<branch>`) first — count 0 fast-forward-pushes as before; on divergence it never plain-pushes, an AskUserQuestion gate offers `git push --force-with-lease` with explicit stop messages for lease rejection (remote gained new commits — review before overwriting) and Cancel (reconcile manually, re-run /optimus:pr); pr README "How It Works" step 4 updated to state the push-if-needed behavior and the force-push confirm gate. F9: one scoping sentence added to Step 1.8 — in an All run, per-repo stop conditions in Steps 2–7 stop only that repo, record the outcome for the Step 8 summary, and continue; only Step 1 gates halt the whole run. F1: friction floor rescoped to "without any intent-related prompting — the Phase 3 preview/confirm still runs". F6: Step 8 heading → "## All PRs/MRs Processed" with a Status column (Created / Updated / Skipped (reason)), which also carries F9's outcome recording. F3 (verifier-preferred remedy): GitHub API default-branch value declared authoritative over Step 1's local detection for Step 5's diffs (stale origin/HEAD note). F4: "as specified in the state 1 rules above" cross-reference dropped from the Problem rule (lean option). F7 (revised): description "same session" → "same conversation" AND line-140 "start of the session" → "start of the conversation"; root README pr row synced to match (commit row's "same session" left for item #17). F2: pr-template.md intro consumers corrected to /optimus:pr + /optimus:code-review (Intent-detection heuristic; grep confirmed tdd SKILL.md never reads the file, code-review SKILL.md:135 does). F5 (revised): pr README Features bullet 36, line 172, and Skill Structure rows 208–209 drop the stale /optimus:tdd consumer claims (default-branch-detection row 210 was already correct — code-review SKILL.md:74 consumes it, no change); tdd README's stale `pr/references/platform-detection.md` shared row removed (its prose "TDD does not perform platform detection" pointer kept — accurate as delegation explanation). No discrepancies — every quote verbatim at current positions; platform-detection.md itself untouched (README wording only, real consumers pr + code-review verified); no validate.sh/expected-outputs pins on any touched phrase (TDD/Workflow summary-heading tokens, mktemp check, and Variant C closing tip all untouched). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#14 prompt** (all 6 findings: F1, F5, F6 found-by-verifier; F2–F4 in ADJUSTED form) — commit `827ae7d`. F1: tool-routing.md made the single CoT source — Template E's header and "When NOT to use", diagnostic #26, SKILL.md Step 4's table row, and Step 6's CoT bullet all drop their hardcoded model lists and defer to the target tool's routing entry (phrased as "unless the entry bars explicit CoT" so entries silent on CoT — GPT-5.x, Gemini, open-weight — resolve unambiguously to allowed), and the near-identical category names reconciled by renaming "Reasoning LLMs" → "General-Purpose LLMs" with a category intro naming the per-entry rule (Claude's auto-calibrated entry authoritative); F2 (revised, polish-not-rescue): Step 4's ~250-word Claude Code routing paragraph converted to a bulleted decision list (execute → H, plan → M, fan-out → N, spec/feature implementation → tdd-or-workflow fork) with the PROMPT-not-plan rule as its own emphasized bullet and the "Run a workflow to…" trigger mechanics left to Template N; F3 (revised fix): the Variant A "approved plan-mode prompt that implemented code in this conversation" case — never true at delivery time — remapped to Variant C, whose condition now reads "a plan-mode prompt — both the approve-and-implement default and the review-only /optimus:tdd route target a new conversation" (no new variant added; skill-handoff.md untouched); F4 (revised fix): Step 8 kept as the canonical marker mechanics, line 34 / Important line 210 / Template L's two paragraphs trimmed to pointer-plus-site-specifics (which-blocks list, each-block-individually rule, and label placement kept; fence-placement re-explanations dropped); F5: Step 9's plan-mode bullet compressed to the paste-into-new-plan-mode-conversation instruction, the default/carve-out routing outcome with do-not-approve inline, and the skill-handoff.md "Plan mode" pointer (Refined-plan mechanics and commit/pr staying advice now live only in the shared doc); F6: the supervision-choice fork moved from Step 9's post-delivery position into Step 4's routing list, with the migration contradiction reconciled — mechanical migrations/codemods are legitimate bare-Template-N work, spec/feature implementation and other production-code fan-outs route to the fork — and Step 9's dynamic-workflow bullet now delivers only the chosen path's handoff. Refuted Step 6 few-shot finding left alone. No discrepancies — every quote verbatim at current positions; no validate.sh/expected-outputs pins on any touched phrase (prompt's only fixture check is files_not_modified; pointer-form closing tip untouched); skill README and root README rows already match (three-target table, migrate-as-fan-out example, and workflow redirect all still hold). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#15 spec-init** (F1 ADJUSTED + F5 found-by-verifier combined, F3 ADJUSTED, F4; F2 skipped) — commit `754757a`. F1+F5 (the dominant multi-repo hole, one combined Step 1 edit): the undefined "appropriate level (workspace root vs. a subproject)" clause replaced with a policy matching downstream discovery — when 2+ repos are detected, ask which repo the product lives in and scaffold inside it by default; if the user insists on the workspace root, warn before scaffolding that /optimus:brainstorm and /optimus:tdd resolve docs/product/ from the repo they run in and will never auto-load a workspace-root cascade, so it would have to be moved into the target repo (the F1-revised warning wording was used over F5's "run those skills from the workspace root" recovery clause, which contradicts brainstorm/tdd Step 1's process-within-the-targeted-repo policy — running them at the workspace root still resolves docs/product/ per-repo); F3 (verifier's revised fix — sanction, not the fragile echo-from-headers): SKILL.md Step 4's inline precedence restatement kept, and sdd-mapping.md's canonical-precedence paragraph now lists two sanctioned restatements (scaffolded doc headers + spec-init Step 4's report), both deferring to the doc as canonical; F4: the tip-variant classification reattached to the recommended next skill ("The recommended next skill (/optimus:brainstorm) is **not** a continuation skill"), correct parenthetical kept, Variant C text untouched (tip-drift guard green). **Skipped:** F2 — description flipped to verb-first via C1 (`315a2a7`) per the cross-item list (note: C1's flip kept "hands off to brainstorm" without the /optimus: command form F2's fix suggested; left alone per the binding skip instruction). No discrepancies beyond the F5-clause note above — every quote verbatim at current positions; no validate.sh/expected-outputs pins on any touched phrase; skill README and root README rows already match (the root row's "brainstorm and tdd read it as steering" claim now holds in the multi-repo branch too); sdd-mapping.md line 10's keep-rules-only-here sentence left as-is (line 40 carries the sanctioned-exception list, same as it already did for the doc headers). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#16 refactor** (F1, F4, F6, F2 in ADJUSTED form, F7 found-by-verifier; F3 + F5 skipped as already done) — commit `e339e45`. F1: guideline-reviewer.md's single-project and monorepo doc lists now include `.claude/docs/skill-writing-guidelines.md` plus a "Skill authoring lens" add-this-instruction bullet mirroring constraint-doc-loading.md's compact routing rule (never judge markdown instruction prose by coding-guidelines.md, never judge code by skill-writing-guidelines.md), making the skill README's lines 6/144/161 dual-lens claims and the root README's line-49 routing claim true for Agent 1 (README wording untouched — the primary fix was taken, not the soften-the-README alternative); F2 (verifier's revised fix): one-line gate at the top of Step 1 ("If the invocation prompt contains `HARNESS_MODE_INLINE`, read Step 2 first — scope is pre-resolved by the orchestrator"), no step renumbering; F4: Step 2's harness enumeration corrected to "Steps 3–6 … apply the fixes mechanically per Step 8's harness-mode paragraph", and the dead "(persistent — fix failed)" skip clause deleted from the interactive Step 8 list (the verifier-sanctioned leaner deletion over the move — harness-mode.md §6 already carries the skip rule for the harness path); F6: Step 8's bare `references/harness-mode.md` §7 pointer prefixed with `$CLAUDE_PLUGIN_ROOT`, closing the file's only validate.sh-invisible reference; F7: harness-mode.md's "Skill-step execution under harness mode" paragraph split into per-skill scope bullets — code-review keeps the branch-diff path, clean-tree rationale, and large-diff-warning skip verbatim; refactor gets "derive analysis areas per Step 3's harness note when scope_files.current is non-empty, else Step 3's normal directory scan with full-project scope" — with the general skip list (user confirmation, scope offers, scope summary) retained above the fork, line 42's code-review-slanted "file discovery via git" fallback generalized to "(per-skill rules below)", and the file-level Contents descriptor updated from "forced branch-diff path" to "per-skill scope rules". **Skipped:** F3 — already resolved by C5 (`90e64b8`), whose pr_description scoping names refactor's opt-out and cites its context-blocks.md note, eliminating the dangling instruction (context-blocks.md line 9 left as-is, now consistent); F5 — description re-tune done via C1 (`315a2a7`, 704→426) per the cross-item list. No discrepancies beyond the F3 already-done state — every other quote verbatim at current positions; no validate.sh/expected-outputs pins on any touched phrase (checked "Steps 3, 4, 5", "persistent — fix failed", "branch-diff", "Skill-step execution", "skill-writing-guidelines"); closing tips untouched (tip-drift guard green). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#17 commit** (all 7 findings: F1, F2, F4–F6, F3 in ADJUSTED form, F7 half-done) — commit `847ecbf`. F2 (the dominant ordering problem): untracked-file handling swapped before message generation (old step 3 → new step 2, old step 2 → new step 3), the new step 2 reads the contents of included untracked files (step 1's gather-changes captures names only) and step 3's analysis explicitly covers them; the split path now routes each commit through steps 4–7 only (per-commit untracked questionnaire dropped — the split proposal already assigns files per commit, and step 4 stays in the loop so the protected-branch split self-heals); the multi-repo steps-2–7 sentence moved to the top of new step 2 and all step cross-references renumbered (step 4's `<type>` source → step 3, step 6's staging source → steps 1 and 2). F1: "Include all" now excludes secret-looking files with individual confirmation for each, and step 6's absolute "Never stage files that look like secrets" gained "unless the user individually confirmed them in step 2". F3 (verifier's revised fix): the variant choice inlined into each step-8 branch — the feature-branch sentence and the "If a pull request is needed" bullet each carry the full "Variant A with `<continuation-skill(s)>` = `/optimus:pr` and `<non-continuation-examples>` = `/optimus:code-review`" substitution spec, the Otherwise bullet keeps Variant C, and the trailing "(either branch above)" catch-all paragraph was deleted (pointer form throughout — no inline `**Tip:**` lines, tip-drift guard unaffected). F4: hook-exists-but-no-`PROTECTED_BRANCHES`-extractable now fails open (treat as unprotected) with a step 5 preview note. F5: step 5 states the per-repo model explicitly ("steps 2–7 run per repo, so each repo gets its own preview and confirmation; label it with a `## repo-name` heading"). F6: description gained the negative trigger "For a message-only suggestion without committing, use /optimus:commit-message." (298→377 chars). F7 **partially pre-resolved by C6-collision (`7dca0f6`)**: the README branch-naming row already reads "shared with the branch, tdd, workflow, and worktree skills" — only the missing `references/skill-handoff.md` shared row was added. Also took the root-README commit-row "same session" → "same conversation" fix explicitly deferred to this item by #13 pr (`d9d795b`). gather-changes.md untouched (shared with commit-message, which needs no content read — read-only message preview per its own step order). No validate.sh/expected-outputs pins on any touched phrase (commit has only the commit-message files_not_modified fixture; tip guard checks inline copies only). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-01 — **#18 worktree** (all 7 findings: F1–F3, F5, F6, F4 in ADJUSTED form, F7 found-by-verifier) — commit `8bdf8e7`. F1: worktree-setup.md's detection guard made detection-only — the canned "Skipping worktree creation — working directly on the current branch" message and "proceed with the standard branch workflow" caller policy (which contradicted SKILL.md Step 2's "Then stop — do not create recursive worktrees" and was overridden by both consumers) replaced with "report the detection result (including `<worktree-path>`) to the calling skill — the caller defines the user-facing message and the next action"; both consumers (worktree Step 2, tdd-worktree-orchestration.md line 9) already define their own message/outcome, so no consumer edits needed. F2: failing-baseline branch added — worktree-setup.md §7 now states baseline failures are pre-existing (same commit as the main workspace), never block or remove the worktree, and must be reported so they aren't attributed to new work; Step 5's report line gains the third outcome "failing (pre-existing — N failures)"; skill README baseline bullet updated from "confirm a healthy starting point" to "record the starting point; pre-existing failures are reported, not blocking". F3: Step 1 "inside each child repo" → "inside the target child repo" (worktree has no per-repo scan; siblings where "each" is correct left alone per the finding). F4 (verifier's revised fix): Step 4's partial restatement replaced with the composing pointer "On failure, follow the reference's **Failure handling** (§8) — cleanup and diagnostics — then stop." F5: category-only AskUserQuestion answers (New feature / Bug fix) now trigger a follow-up asking for a few words describing the task before slug generation. F6: one-line multi-repo note after the Step 5 template — prefix all report paths with the target repo directory (e.g., `cd <repo>/.worktrees/<worktree-dir>`). F7 (conditional-report-line option over the stop-staging option — doc-only, no shared-behavior change for tdd): when §3 modified `.gitignore`, the report adds ".gitignore updated to ignore .worktrees/ (staged, not committed)" after the Main-workspace line, and the intro's "stays untouched" became "stays on its original branch"; tdd's mirror report line left alone (out of item scope, §3 behavior unchanged). No discrepancies — every quote verbatim at current positions (C6-collision's pointer swap didn't touch any anchor); no validate.sh/expected-outputs pins on any touched phrase (checked "each child repo", "Tests: passing", "unchanged", "stays untouched", "standard branch workflow", "no test command detected", "diagnostic information"); closing tip untouched (tip-drift guard green); README worktree examples still valid template outcomes; root README worktree row already matches. All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-02 — **#19 brainstorm** (all 8 findings: F1–F7, F8 found-by-verifier; small targeted fixes per the well-tuned guidance) — commit `988d615`. F1 (the convention-conflict medium, first remedy chosen — align with the canonical convention rather than document a divergence): prose deliverables no longer route through the /optimus:tdd review-only carve-out — the "Prose deliverable" note now follows skill-handoff.md's default plan-mode flow (iterate, approve, implement in the same conversation; no `### Refined plan` append, no execution prompt), stating the three adjustments to the Medium-to-large branch, and the routing sentence sends Claude to the note *before* the branch so the carve-out closer/steps aren't emitted linearly; skill-handoff.md needed no edit (its line-78 "code paths" scoping is now exactly true); the closing "why plan mode is used review-only" line rescoped to "on the /optimus:tdd path". F2: resolved inside F1's rewrite with the fix's principle retargeted — since the prose path no longer has an execution prompt, the temporally-impossible "After `<deliverable-path>` is produced, recommend /optimus:commit" imperative became an instruction to embed the recommendation + substituted Variant A tip in the emitted *plan-mode* prompt, so the executing conversation delivers them (pointer form kept — no literal **Tip:** line added, tip-drift guard unaffected). F3: Step 4 list gains Context (after Goal) and Open questions (after Out of scope), matching spec-format.md. F4: the refactor/test-only handoffs now include `<spec-path>` and pass its scope/key decisions as the next skill's scope/path argument (tip text untouched on both lines). F5: Medium-to-large heading appends "or anything not matching Small" (the finding's "otherwise" option). F6: all four `<file-path>` occurrences (Step 6 report, Small, workflow alternative, plan-prompt Starting Hints) renamed `<spec-path>`, and the substitution note broadened to cover Steps 6–7 wholesale (also closing the verifier's uncovered-blockquote remark — the three-steps pointer was already covered by its own "`<doc-path>` = `<spec-path>`" clause since C6-planmode). F7: sdd-mapping.md's "Step 4 writes" → "writes" (the drop-the-number future-proof option). F8: Step 5 checks for a pre-existing target file, asks overwrite vs suffixed filename (`-2`, `-3`, … keeps the slug regex valid) via AskUserQuestion, and carries any existing `### Refined plan` section over on overwrite. Discrepancy (procedural only, problems all still live): F1/F2/F6's quoted anchors had been reshaped by C6-planmode's hoist (`ec482a9`) — the inline carve-out block/blockquote are now pointers, so the "Do not approve"/"fresh conversation to run /optimus:tdd" contradiction reached the prose path via the pointered canonical blocks instead of inline text; fixes applied against the current structure. Skipped: the description finding was never raised for this skill (C1 cross-item note covered only the C1-side docs/specs alignment, already done). No validate.sh/expected-outputs pins touched ("## Scenarios"/"### Scenario:" live in spec-format.md/tdd, untouched; Variant C tips byte-identical); skill README and root README rows still match (prose sub-path and overwrite check sit below their abstraction level; the review-only plan-mode row describes the unchanged code path). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-02 — **#20 code-review-deep** (F2, F6–F8 + F3 in ADJUSTED form; F1, F4, F5 skipped as already done) — commit `f0a55a9`. F2: README Requirements gains "Green test suite at start (or `--allow-red-baseline`)", Usage lists `--allow-red-baseline` (mirroring refactor-deep's line), and a one-sentence leftover-progress-file note points at `--resume` / the CLI `init --force` recovery — the fix's "list `--force` in Usage" clause was adapted because C6-cli (`2dcb349`) made `--force` a cli.py-init-only flag, so presenting it as a skill flag would reintroduce F1's contradiction; F3 (verifier's revised fix): the SKILL.md Tip's first paragraph (resume/cap-raise/archive semantics duplicated from Steps 4/6) trimmed to a pointer, the unique second-opinion paragraph kept, and its wrong subcommand fixed (`init` → `resume`, matching cli.py cmd_resume's "No progress file" error); F6: orchestrator-loop-single.md's dispatch template line `Phase: <skill>` renamed to the defined `<base-skill>` (rename option; paired-loop's literal Phase lines untouched, nothing pins the line — checked validate.sh, expected-outputs.yaml, test_skill_contract.py); F7: step 3's maintainer-facing rename/dev-mirror prose replaced with the execution-time clause (exact prefixes required; `commit_checkpoint`'s un-stage step matches them), and the dev-mirror/rename-sync guidance moved to a comment above `_HARNESS_STATE_EXCLUDES` in scripts/harness_common/git.py (the finding's git.py-comment option; comment-only, coding-guidelines read); F8: README `cap` row reworded to the archived-then-re-run guidance, matching the Tip and cli.py cmd_final_report. **Skipped:** F1 — resolved by C6-cli (`2dcb349`): the "re-invoke this skill with `--force`" bullet and the contradicting "(no separate user-visible orchestrator flag is needed)" parenthetical no longer exist; Step 4 defers to harness-init-resume.md, whose line 29 states the corrected semantics; F4 — description dedup done via C1 (`315a2a7`) per the cross-item list (verified: the duplicate isolated-subagent sentence is gone from the frontmatter); F5 — the quoted "higher cap" sentence no longer exists in SKILL.md (C6-cli hoisted it) and #12 (`36ab6b6`) already applied the revised wording in harness-init-resume.md line 19 ("Pass `<cap-flag> N` through whenever the user supplied it"). No other discrepancies — remaining quotes verbatim at current positions; no validate.sh/expected-outputs pins on any touched phrase; the Important section's Variant A closing tip untouched (tip-drift guard green); root README code-review-deep row already matches (no baseline/cap-resume claims). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-02 — **#21 branch** (all 6 findings: F1–F5, F6 found-by-verifier; small targeted fixes per the well-tuned guidance) — commit `bef9238`. F1: Step 5's `/optimus:tdd` bullet caveated per the proposed fix — note that tdd creates its own branch from the current one, warn the user a second branch will stack on the one just created, and flag that this skill wasn't needed if TDD was the plan — resolving the SKILL↔README contradiction with "When NOT to Use" (README unchanged, already correct); F2: the hardcoded Variant B replaced with conditional selection in the house pointer-form style (commit:134-138, how-to-run:215): Variant A when only `/optimus:commit` was recommended, Variant C when only tdd/worktree, Variant B only for a mix (no inline `**Tip:**` line, so the tip-drift guard is untouched); F3: one line after the diff commands — untracked files don't appear in `git diff`; use `git status --short` paths and read the new files if names alone are ambiguous; F4: "the shared reference" → "`branch-naming.md` (read in Step 3)"; F5: Step 4 gains "In a multi-repo workspace, name the target repo in the report, e.g. Created `<branch-name>` in `<repo>` from `<original-branch>`."; F6: Step 1's one-changed-repo silent targeting now yields when the inline description or conversation clearly identifies a different repo (its clean tree is the starting-fresh case; the other repo's changes stay untouched), and the skill README's multi-repo Features bullet gained "(an explicit description naming a different repo wins)". No discrepancies — every quote verbatim at current positions (C6-collision's pointer swap in Step 3 touched no anchor); no validate.sh/expected-outputs pins on any touched phrase (branch's only fixture token is "feat/"; the 'Variant A' wiring token belongs to skills/handoff/SKILL.md); root README branch row already matches (no next-step or repo-targeting claims). All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
- 2026-07-02 — **#22 commit-message** (all 6 findings; F4/F5 in ADJUSTED form) — commit `8488b72`. F1: the closing /optimus:pr branch no longer recommends a PR from an uncommitted state — it now reads "commit first (`/optimus:commit`), then `/optimus:pr` ... — pr never commits working-tree changes" (Variant A tip unchanged, still lists both continuation skills); F2: skill README 'How It Works' drops the false "operates entirely on diff output" sentence, documents conversation-context capture (the *why* the diff can't show), and a matching Features bullet was added — root README row 93 already advertised the behavior, no change needed there; F3: the "too broad" Important bullet deleted and Step 3's "naturally split into multiple commits" aligned to the reference's "span multiple concerns" (verified unpinned: validate.sh has zero commit-message matches, fixture is files_not_modified only); F4 (verifier's revised skill-neutral wording): gather-changes.md 'Gather changes' gains "If the commands show no staged, unstaged, or untracked changes, inform the user there are no local changes to work with and stop." — fixes the single-repo clean-worktree gap for both consumers (#17's changelog confirms it left gather-changes.md untouched); F5 (verifier's revised missing-input framing): conventional-commit-format.md 'Analyze Changes' notes untracked files appear as names only (`git status --short` produces no diff) — read contents when needed for type/scope; F6: description gains "Best run in the conversation where the changes were implemented, so the message body captures the why." (198→309 chars). Skipped the verifier's soft F6 suggestion to mirror the clause into /optimus:commit's description — outside this item's scope and #17 already reworked that description (drive-by risk). Preserved everything in the do-not-churn list; no discrepancies — all six quotes verbatim in current files. All three suites green (validate.sh 18 passed, jq SKIPs environmental; test-hooks 45; pytest 410).
