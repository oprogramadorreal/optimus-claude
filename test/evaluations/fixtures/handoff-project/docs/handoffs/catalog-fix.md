# Prior catalog handoff

Previous decision: preserve case-sensitive IDs because external clients persist them. The rejected alternative was a global lowercase-key migration.

This prior version contains unique local analysis that must remain recoverable when overwritten: `PRIOR_LOCAL_DECISION_719`.
