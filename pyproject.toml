[tool.pytest.ini_options]
# importlib mode allows test files with the same name across different test trees
# without sys.modules collisions. Kept for general robustness.
addopts = "--import-mode=importlib"
